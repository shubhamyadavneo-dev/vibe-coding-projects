
Build a **Todo Dashboard (SaaS UI)** using **React + TypeScript + Tailwind**.

* Frontend only, use **localStorage**
* Pages: Login (mock), Dashboard (stats), Todo List (search + filter), Create/Edit Todo
* Features: CRUD, status toggle, priority, due date
* Layout: **Sidebar (left) + content (right)**
* UI must match **attached design** and **bg-design.md**
* Code should be **modular, reusable, type-safe**

**Storage:**

```ts
todos: [{ id, title, description, priority, dueDate, completed, createdAt }]
user: { isLoggedIn }
```

**Goal:** Clean, modern, responsive SaaS-style app

